# ActiveSync protocol (optional, not needed for IMAP)
# py-eas-client is not available on PyPI
# If you need ActiveSync, see LOCAL_MODELS.md for alternatives

# HTTP and XML handling
requests>=2.32.5
lxml>=6.0.2
python-dateutil>=2.9.0

# AI integration
anthropic>=0.76.0  # For Anthropic Claude API
# For local LLMs via Ollama, requests is used (already included below)

# Configuration and utilities
pyyaml>=6.0.3
python-dotenv>=1.2.1

# Logging and monitoring
colorlog>=6.10.1

# Embedding and clustering (for embedding-based analysis mode)
sentence-transformers>=5.2.0
scikit-learn>=1.8.0
hdbscan>=0.8.41
numpy>=2.4.1
managesieve>=0.8.1
